{{page-title "Favorites"}}
<div class="w-[450px]">
  {{#each this.model as |song|}}
    <div class="flex justify-between">
      <div class="flex items-center">
        <button type="button" class="mr-3" {{on "click" (fn this.deleteFavorite song)}}>
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-red-500">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9.75 9.75l4.5 4.5m0-4.5l-4.5 4.5M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </button>
        {{song.title}} <span class="text-teal-200 text-sm ml-1">by {{song.band.name}}</span>
      </div>
      <div class="flex"><StarRating @song={{song}} @rating={{song.rating}} /></div>
    </div>
    {{else}}
    <h2 class="text-center my-10">Aucun favori n'a été sélectionné</h2>
    <LinkTo @route="songs" class="text-sm">Retourner sur la liste des musiques</LinkTo>
  {{/each}}
</div>